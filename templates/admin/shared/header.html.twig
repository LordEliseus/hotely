<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('home') }}">HOTELY</a>
    <div class="collapse navbar-collapse" id="navcol-1">
        <ul class="nav navbar-nav">
            <li class="nav-item"><a href="#"  class="nav-link"><i class="fa fa-line-chart"></i>&nbsp;Dashboard</a></li>
            <li class="nav-item"><a href="{{ path('admin_ads_index') }}" class="nav-link"><i class="fa fa-hotel"></i>&nbsp;Annonces</a></li>
            <li class="nav-item"><a href="{{ path('admin_booking_index') }}" class="nav-link"><i class="fa fa-folder"></i>&nbsp;Réservations</a></li>
            <li class="nav-item"><a href="{{ path('admin_comment_index') }}" class="nav-link"><i class="fa fa-comments"></i>&nbsp;Commentaires</a></li>
            <li class="nav-item"><a href="#" class="nav-link"><i class="fa fa-users"></i>&nbsp;Utilisateurs</a></li>
        </ul>
        <ul class="nav navbar-nav ml-auto">
            <li class="nav-item">
                <a href="{{ path('home') }}" target="_blank" class="nav-link">
                    <i class="fa fa-eye">&nbsp;Voir le site</i>
                </a>
            </li>
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropDownLink">
                        <img src="{{ asset('img/el.jpg') }}" alt="" class="avatar avatar-mini" alt="{{ app.user.fullName }}">
                        {{ app.user.fullName }}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropDownLink">
                        <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}